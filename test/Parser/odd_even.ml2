(* This is a comment and should be ignored.
  (* Comments may be nested. *)
*)

let rec odd (x : int) : bool =
  if x = 0 then false else even (x - 1)
and even (x : int) =
  if x = 0 then true else odd (x - 1)

let _ = odd 5
let _ = even 5
